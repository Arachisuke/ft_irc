// tester les users affiche en entrant en second dans un channel ca n'affiche pas le membre qui vient actuellement d'entrer ca affiche que le premier membre @@@

// privmsg -> j'envoie que un mot et pas tout le msg. @@@@@
// privmsg -> channel et clients @@@@@
// privmsg -> double USER, et channel. @@@@

// JOIN -> PART @@
// JOIN -> multiple @@@@
// JOIN -> MODE.


// PART -> a tester @@@

// le parsing a gerer quand ya : lire la doc etc.



// PART -> quitter, le erase de la liste des users et de sa propre liste des channels, et s'il est le dernier delete le channel. @@@



// list des channels via server  @@@@
// list des users du channel via servers@@@@ 
// list des channels via user. @@@@






// je me suis tormpe dans le mdp il a pas change c normal ? peut etre oui vu que javais pas fini la norme du coup ca a fait un mauvais mdp @@@@


// jai ressaye ca a coupe le fd, normal du coup je me reconnecte ca marche aps et je fais segfault le server ..
// aussi c'est que les autres ont ete alerte par mon nick, mais du coup j'ai pas reussi a me co, doit-je les informer meme quand je rate ou je dois attendre d'avoir fini la registration de maniere safe.


// // segfault quand je fais part ? @@@@
// // a reason "k" ? alors que j'ai rien envoyer ??? @@@

// ??????????????? probleme en cours celui juste au dessus.
// tester une condition differente ? @@@





// relire join t'as deconne sur le code relis bien c'est channel liste etc et tu supprimais liste qui etait une ref.






// corriger JOIN @@@@
// nick already on use ....@@@@@
// corriger JOIN 0 @@@@
// test le NICK faut que les gens du meme channel que lui et lui meme recoit, sil est dans aucun lui seul recois  @@@@@
// controle C client, reaction c'est bon@@@@

// copier join 0 dans part @@@@@
// mauvais mot de passe ca me deco, je me reco ca segfault... @@@@

  // vrai puis faux et je tente -> erreur car je renvoie une erreur mais je continue ma             boucle, et du coup jai free le client mais je lis dans le bufferClient ...du coup try    and. catch. @@@@@
  // j'ai regler ca mais maintenant le controle C dans le client fait des erreurs ...@@@
  
  // faux puis vrai et je tente @@@
  // faux et je tente@@@@@
  // vrai et je tente @@@@


// valgrind sans arg, invalide free pour quelle raison je ne fais qu'un return. !!@!@!

// controle C dans le server ... leaks ? !@!@! ce n'est pas moi qui fuite a ce qu'il parait



// le SIGQUIT -> // @@@@@@

// parsing : @@@@


// // join plus logique...@@@@
// // j'ai rendu part aussi plus logique, part est different de JOIN. @@@@

// QUIT 
// // continuer pour retirer le client des channels. @@@@


// tester QUIT -> S'il quitte bien envoie a tlm le msg, et s'il y a pas de leaks et voila.
// tester NOTICE -> no erreur tester les 4-5 cas et s'il envoie bien.


// join 0 
// part 
// topic
// quit


// join -> si jessaye de join un channel dont je suis deja membre mais qu'en deuxieme argument je ne suis pas membre, il ne calcule pas et return, la j'ai mis continue. @@@@

// quit -> envoyez qu'un quit meme si j'ai deux channel en commun  @@@@

// quit? -> leaks. invalid read. du a la boucle faut throw une exception.@@@


// quit -> double msg du throw et de send. @@@@
// // reverifie le cas du faux password les msg et les leaks. @@@@

// // 


// notice join privmsg part -> multiple with error continue to check other arg.  @@@ a tester @@@@@

// // retester le msg closing link et quit. @@@@

// // faire full test puis controle C, ya un leaks a priori
// // le test c join part privmsg notice quit.


// // mode a faire !!!! -> reste qu'a implementer le setter et une erreur.




// //probleme de retrait du channel lors d'un ctrl+c

// ➜  ft_irc git:(server) ✗ nc localhost 1080
// PASS p^M
// NICK po^M
// :!@localhost NICK : po
// USER p p p p^M
// :HuecoMundo 001 po :Welcome to the Hueco Mundo Network, po!~p@localhost
// :HuecoMundo 002 po :Your host is HuecoMundo, running version 4.3.3
// :HuecoMundo 003 po :This server was created Sep 10 2025
// :HuecoMundo 004 po HuecoMundo version-4.3.3 itkol :are supported by this server
// JOIN #42^M
// :po!p@localhost JOIN #42
// :HuecoMundo 353 po = #42 : po 
// :HuecoMundo 366 po #42 :End of /NAME list
// MODE #42^C
// ➜  ft_irc git:(server) ✗ nc localhost 1080
// PASS p^M
// NICK po^M
// :!@localhost NICK : po
// USER p p p p^M
// :HuecoMundo 001 po :Welcome to the Hueco Mundo Network, po!~p@localhost
// :HuecoMundo 002 po :Your host is HuecoMundo, running version 4.3.3
// :HuecoMundo 003 po :This server was created Sep 10 2025
// :HuecoMundo 004 po HuecoMundo version-4.3.3 itkol :are supported by this server
// JOIN #42^M
// :HuecoMundo 443 po!p@localhost JOIN #42 is already on channel







== = = = = = = = = = = = ========= = = = = = = = = ====== ==== =. ==. = == = =. == = = = = ===. = === = = = =



NEW TEST :

-> name operator, le @ est ajoute a chaque channel cree par l'user du coup si je JOIN plusieurs channel, l'USER a pour chaque channel un @ en plus
-> NICK already on USE, si le gars c'est un operator, il aura @ du coup je pourrais plus les comparer.


-> ordre des clients dans NAME LIST est ce normal ? 



-> NICK successfullNick dans plusieurs channel, il faut l'envoyer qu'a un seul client meme si ya plusieurs channel en commun. @@@@



-> QUIT leak.... @@@@

-> QUIT msg entre parenthese trop d'espace. @@@@




-> QUIT, me reconnecte et faire JOIN ca leak. @@@

-> jai join un channel ya encore kenpachi, alors qu'il a quit avant....@@@@



// QUIT + controle C server double free etc etc...leak etc etc @@@@@

// Verifie tout les endroit ou je nettoie les repertorier avec le point rouge, ensuite reverifie la logique a chaque fois et la correlation entre eux.
// JOIN QUIT CLOSE CLIENT FINISH PART
// JOIN QUIT CLOSE PART @@@@@


// refaire le test de leak avec toute les fonctions a free tu les utilises toutes + controle C seerver ou client










// premier NICK le client ne recoit rien  @@@@@


// IRSSI -> JOIN 0 fail car il parse en lui mettant un diez. @@@@

// si loperator quitte, est ce que le chanelops se transmet....@@@@@







//15:11 -!- Cannot join to channel TOPIC (Bad channel mask)

// @ => JOIN display client @@@ / basic type message a change // PRIVMSG NOTICE, KICK, 

// JOIN #channel1 1212 #channel pour le mode k, pour bien parser. 
// -> Mode k, avoir un int nbrkey incrementer quand j'ai utilise le premier 
// cas -> good key or not@@@@


MODE K -> JOIN@@@

     Command: JOIN
  Parameters: <channel>{,<channel>} [<key>{,<key>}]
  Alt Params: 0


  channel = cmd[1] => SPLIT
  key = cmd[2] => SPLIT

if -> 



// est ce que tu genere vraiment un password dans mode. @@@


PART corriger // part y avais pas besoin -> car je bouclais sur quelque chose que je supprimais pas.
 #1 boucle decroissant
 #2 channel[i] = NULL, apres l'erase channel[i] du vector du coup j'accedais a un autre index du vector.
QUIT
error #2 juste au dessus, corriger.
CLOSE
error #2


 
// erreur -> JOIN multiple => PART multiple.  // il marche ....
// JOIN Multiple -> JOIN 0 ...





-- - - - ---------------------- ---------------------------- --------------------------------------- --------------------- --------------------- ----------------- --------------- ----------------- -------------  ----------------- ----------- -----------




KICK -> multi message ?

TOPIC -> essayer de change le topic op/notOP
Invite -> inviter 
MODE -> I T K O L 

KICK TOPIC -> mode O T
TOPIC -> en mode T et pas en MODE T pour le modifier ou non OP/notOP






PASSWORD -> faux vrai faux mdp @@@
PASSWORD -> vrai faux mdp. @@@@
NICK -> already, succesfful -> premiere fois, seconde fois, channel multiple.
USER -> envoyez pas mal d'argument.
JOIN -> tenter avant le register 
JOIN -> multichannel, channel faux, channel alreadyIN
JOIN -> 0
QUIT -> multi message ?
PART -> multichannel, channel faux, channel notIN
PRIVMSG NOTICE -> notIN, multichannel/user faux user faux channel 
PING -> argument long ou bizarre
JOIN -> I L K


tout en verifiant les LEAK -> QUIT -> CLOSECLIENT -> FINISH \



PARSING de chaque commande avec ou sans argument -> QUIT KICK PING PART 



QUIT -> IRSSI bizarrre
JOIN -> invalid read size, je n'arrive pas a reproduire le cas.
-> juste un make pas un make re, tu lance le premier client fait un JOIN avec UN channel ! et c'est pas sur, le pb vient de l'initialisation du constructeur


// reverifier lheure de irssi et netcat.


// corriger les commentaires.


// test alreadyinUSE, avec channel, j'ai fais un mixte de changement de l'operateur ect, je n'ai aps reussi a PART 
// PARRT a ete MODIFIER a reverifier entierement.
// PART a corriger il a des invalid read.


// bien faire des removes client, car ca enleve de tout , operator user etc.